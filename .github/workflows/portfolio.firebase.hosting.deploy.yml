# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: PORTFOLIO Deploy to Firebase Hosting on PR and MERGE

on:
  push:
    branches: [master]
    paths: ["portfolio/**"]
  pull_request:
    branches: [master]
    paths: ["portfolio/**"]
  workflow_dispatch:

defaults:
  run:
    working-directory: portfolio

jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout - Checkout to working directory
        uses: actions/checkout@v2
      - name: CI - Install dependencies
        run: yarn ci
      - name: Build - Build Project
        run: yarn build
      - name: Export - Export Project
        run: yarn export
      - name: Firebase Extended Action
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entrypoint: portfolio
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_JESUSEYITAN_8DEA1 }}"
          projectId: jesuseyitan-8dea1
          channelId: live
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
